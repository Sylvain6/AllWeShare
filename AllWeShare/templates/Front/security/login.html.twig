{% extends 'base.html.twig' %}

{% block body %}
    {% if error %}
        <div class="alert alert-danger">
            {{ error.messageKey }}
        </div>
    {% endif %}


    <div id="subscribe_form">
        <div class="row">
            <div class="col-md-12">
                <form action="{{ path('app_front_security_login') }}" method="post">
                    <h4><i class="fa fa-lock" aria-hidden="true"></i> Connexion</h4>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input class="form-control" type="text" id="email" name="_username" value="{% if error %}{{ error.token.user }}{% endif %}"/>
                    </div>
                    <div class="form-group">
                        <label for="password">Mot de passe</label>
                        <input class="form-control" type="password" id="password" name="_password" />
                    </div>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                    <i class="fa fa-sign-in" aria-hidden="true"></i>
                    <button type="submit" class="btn btn-primary"> Connexion
                    </button>
                </form>
            </div>
        </div>
    </div>

{% endblock %}