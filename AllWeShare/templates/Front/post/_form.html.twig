{{ form_start(form, { 'attr' : { 'id': 'postForm' }}) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <button type="submit" id="submitPost" class="btn btn-outline-primary float-right">{{ button_label|default('Share !') }}</button>

{{ form_end(form) }}
