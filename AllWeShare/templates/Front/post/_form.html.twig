{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <button type="submit" class="btn btn-allWeShare float-right">{{ button_label|default('Share !') }}</button>

{{ form_end(form) }}
