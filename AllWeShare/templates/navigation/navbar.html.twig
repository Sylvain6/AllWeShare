<nav class="navbar navbar-expand-lg bg-light">
    <div class="col-md-2">
        <a class="navbar-brand" href="{{ path('post_index') }}">
            <img src="{{ asset('images/allweshare-logo.png') }}" class="img-fluid"width="166" height="166">
        </a>
    </div>
    <div class="collapse navbar-collapse">
        <div class="form-inline col-md-3 offset-md-9">
            <ul class="navbar-nav mr-auto align-center">
                {% if(app.user) %}
                    <li class="nav-item">
                        <a class="nav-link u-c-b" href="{{ path('post_index') }} ">What's new ?</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link primary-color" href="{{ path('admin_user_index') }} ">Users</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link primary-color" href="{{ path('admin_post_index') }} ">Posts</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link primary-color" href="{{ path('logout') }} ">Deconnection</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link u-c-b" href="{{ path('app_front_security_login') }} ">Sign in</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link primary-color" href="{{ path('app_front_security_registration') }} ">Sign up</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>