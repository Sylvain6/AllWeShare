
<nav class="navbar navbar-expand-lg bg-light">
    <div class="col-md-2">
        <a class="navbar-brand" href="{{ path('index') }}">
            <img src="{{ asset('images/allweshare-logo.png') }}" class="img-fluid"width="166" height="166">
        </a>
    </div>
    <div class="collapse navbar-collapse">
        <div class="form-inline offset-md-8">
            <ul class="nav nav-tabs ">

                {% if(app.user) %}
                    <li class="nav-item">
                        <a  class="btn nav-link u-c-b {{ ( app.request.attributes.get('_route') == 'post_index' ) ? 'active' }}" href="{{ path('post_index') }}">What's new ?</a>
                    </li>


                    <li>
                        <div class="dropdown">
                            <a class="btn nav-link u-c-b dropdown-toggle {{ ( app.request.attributes.get('_route') == 'user_account') ? 'active' }}"
                               id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Account
                            </a>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                <a class="btn nav-link u-c-b" href="{{ path('user_account') }}">My profile</a>
                                <a class="btn nav-link u-c-b" href="#">My groups</a>
                            </div>
                        </div>
                    </li>


                    <li class="nav-item">

                        <a class="btn nav-link primary-color {{ ( app.request.attributes.get('_route') == 'back_user_index' ) ? 'active' }}" href="{{ path('back_user_index') }}">Admin</a>
                    </li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link u-c-b" href="{{ path('app_front_security_login') }} ">Sign in</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link primary-color" href="{{ path('app_front_security_registration') }} ">Sign up</a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>